'use strict';

var os = require('os');

console.log(os.hostname());

console.log(os.tmpDir());

console.log(os.platform());

console.log(os.type());

console.log(os.release());

console.log(os.arch());
